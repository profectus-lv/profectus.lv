{% if siteelements.features.json_ld %}
{# Page description #}
{% set description -%}{% getShortDescription %}{%- endset %}
{# Page type detection #}
{% set isPost = tags and ('posts' in tags) %}
{% set isAuthorPage = tags and ('authorcard' in tags) %}
{% set pageUrl = page.url | absoluteUrl %}
{% set imageSrc = image or siteconfig.openGraphDefaultImage %}
{% set isProfilePage = (not isPost and not isAuthorPage and profilePage) %}

{# Detect events: any tag included in sitetags.events #}
{% set isEvent = false %}
{% if tags %}
    {% for evtag in sitetags.events %}
        {% if evtag in tags %}{% set isEvent = true %}{% endif %}
    {% endfor %}
{% endif %}

{# Unified author derivation (for both posts and author pages) #}
{% set activeAuthorId = author or siteconfig.author %}
{% set activeAuthorData = siteauthors[activeAuthorId] %}
{% set activeAuthorDisplayName = activeAuthorData.fullName or activeAuthorId %}
{% set authorImage = activeAuthorData.photo or siteconfig.image %}
{% set authorPageUrl = (siteconfig.pagination.authorLinkPrefix ~ (activeAuthorId | empSlugify) ~ '/') | absoluteUrl %}

{# Generate keywords from tags #}
{% if isPost %}
    {% set keywords = tags | getKeywords %}
{% endif %}

{# Author sameAs links #}
{% set sameAs = [activeAuthorData.website] if activeAuthorData.website else [] %}
{% set social = activeAuthorData.social or {} %}
{% for key, value in social %}
    {% set cfg = socialconfig[key] %}
    {% set sameAs = sameAs.concat([cfg.prefix ~ value]) %}
{% endfor %}

{# Publisher sameAs links #}
{% set publisherSameAs = [] %}
{% for key, value in siteelements.social %}
    {% set cfg = socialconfig[key] %}
    {% set publisherSameAs = publisherSameAs.concat([cfg.prefix ~ value]) %}
{% endfor %}

{# Publisher context #}
{% set publisherData = siteauthors[siteconfig.author] %}
{% set publisherName = publisherData.fullName or siteconfig.author %}
{% set publisherImage = publisherData.photo or siteconfig.image %}

<script type="application/ld+json">
{
    "@context": "https://schema.org",
    "@graph": [
        {
            "@type": "WebSite",
            "@id": "{{ '/' | absoluteUrl }}#website",
            "name": "{{ siteconfig.title | jsonEscape | safe }}",
            "url": "{{ '/' | absoluteUrl }}",
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ siteconfig.description | jsonEscape | safe }}",
            "logo": {
                "@type": "ImageObject",
                "url": "{% imageTransformAbsoluteUrl siteconfig.image, 1200, 'webp' %}"
            },
            "publisher": {
                "@type": "{{ siteconfig.authorType }}",
                "name": "{{ publisherName | jsonEscape | safe }}",
                "url": "{{ '/' | absoluteUrl }}",
                {% if publisherData.bio %}"description": "{{ publisherData.bio | jsonEscape | safe }}",{% endif %}
                {% if publisherData.email %}"email": "{{ publisherData.email | jsonEscape | safe }}",{% endif %}
                {% if publisherSameAs | length %}"sameAs": {{ publisherSameAs | dump | safe }},{% endif %}
                "{{ 'image' if siteconfig.authorType == 'Person' else 'logo' }}": {
                    "@type": "ImageObject",
                    "url": "{% imageTransformAbsoluteUrl publisherImage, 1200, 'webp' %}"
                }
            }
        }
        {% if isEvent %}
        ,{
            "@type": "Event",
            "@id": "{{ pageUrl }}#event",
            "name": "{{ title | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            "isPartOf": { "@id": "{{ '/' | absoluteUrl }}#website" },
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ description | jsonEscape | safe }}",
            "image": [
                "{% imageTransformAbsoluteUrl imageSrc, 1200, 'webp' %}"
            ],
            "startDate": "{{ page.date | isoDateTime }}",
            {% if place %}"location": {
                "@type": "Place",
                "name": "{{ place | jsonEscape | safe }}"{% if address %},
                "address": "{{ address | jsonEscape | safe }}"{% endif %}
            },{% endif %}
            "organizer": {
                "@type": "{{ siteconfig.authorType }}",
                "name": "{{ publisherName | jsonEscape | safe }}",
                "url": "{{ '/' | absoluteUrl }}",
                {% if publisherData.bio %}"description": "{{ publisherData.bio | jsonEscape | safe }}",{% endif %}
                {% if publisherData.email %}"email": "{{ publisherData.email | jsonEscape | safe }}",{% endif %}
                {% if publisherSameAs | length %}"sameAs": {{ publisherSameAs | dump | safe }},{% endif %}
                "{{ 'image' if siteconfig.authorType == 'Person' else 'logo' }}": {
                    "@type": "ImageObject",
                    "url": "{% imageTransformAbsoluteUrl publisherImage, 1200, 'webp' %}"
                }
            }
        }
        {% elseif isPost %}
        ,{
            "@type": "BlogPosting",
            "@id": "{{ pageUrl }}#blogposting",
            "headline": "{% if title %}{{ title | jsonEscape | safe }} - {% endif %}{{ siteconfig.title | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            "isPartOf": { "@id": "{{ '/' | absoluteUrl }}#website" },
            "mainEntityOfPage": "{{ pageUrl }}",
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ description | jsonEscape | safe }}",
            "image": [
                "{% imageTransformAbsoluteUrl imageSrc, 1200, 'webp' %}"
            ],
            "datePublished": "{{ page.date | isoDateTime }}",
            {% if updated %}"dateModified": "{{ updated | isoDateTime }}",{% endif %}
            "keywords": {{ keywords | dump | safe }},
            "author": {
                "@type": "Person",
                "name": "{{ activeAuthorDisplayName | jsonEscape | safe }}",
                "url": "{{ authorPageUrl }}",
                {% if activeAuthorData.bio %}"description": "{{ activeAuthorData.bio | jsonEscape | safe }}",{% endif %}
                {% if activeAuthorData.email %}"email": "{{ activeAuthorData.email | jsonEscape | safe }}",{% endif %}
                {% if sameAs | length %}"sameAs": {{ sameAs | dump | safe }},{% endif %}
                "image": {
                    "@type": "ImageObject",
                    "url": "{% imageTransformAbsoluteUrl authorImage, 1200, 'webp' %}"
                }
            },
            "publisher": {
                "@type": "{{ siteconfig.authorType }}",
                "name": "{{ publisherName | jsonEscape | safe }}",
                "url": "{{ '/' | absoluteUrl }}",
                {% if publisherData.bio %}"description": "{{ publisherData.bio | jsonEscape | safe }}",{% endif %}
                {% if publisherData.email %}"email": "{{ publisherData.email | jsonEscape | safe }}",{% endif %}
                {% if publisherSameAs | length %}"sameAs": {{ publisherSameAs | dump | safe }},{% endif %}
                "{{ 'image' if siteconfig.authorType == 'Person' else 'logo' }}": {
                    "@type": "ImageObject",
                    "url": "{% imageTransformAbsoluteUrl publisherImage, 1200, 'webp' %}"
                }
            }
        }
        {% elseif isAuthorPage %}
        ,{
            "@type": "ProfilePage",
            "@id": "{{ pageUrl }}#profilepage",
            "name": "{% if title %}{{ title | jsonEscape | safe }} - {% endif %}{{ siteconfig.title | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            "isPartOf": { "@id": "{{ '/' | absoluteUrl }}#website" },
            "mainEntity": { "@id": "{{ pageUrl }}#author" },
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ description | jsonEscape | safe }}"
        },
        {
            "@type": "Person",
            "@id": "{{ pageUrl }}#author",
            "name": "{{ activeAuthorDisplayName | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            {% if activeAuthorData.bio %}"description": "{{ activeAuthorData.bio | jsonEscape | safe }}",{% endif %}
            {% if activeAuthorData.email %}"email": "{{ activeAuthorData.email | jsonEscape | safe }}",{% endif %}
            {% if sameAs | length %}"sameAs": {{ sameAs | dump | safe }},{% endif %}
            "image": {
                "@type": "ImageObject",
                "url": "{% imageTransformAbsoluteUrl authorImage, 1200, 'webp' %}"
            }
        }
        {% elseif isProfilePage %}
        ,{
            "@type": "ProfilePage",
            "@id": "{{ pageUrl }}#profilepage",
            "name": "{% if title %}{{ title | jsonEscape | safe }} - {% endif %}{{ siteconfig.title | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            "isPartOf": { "@id": "{{ '/' | absoluteUrl }}#website" },
            "mainEntity": { "@id": "{{ pageUrl }}#profile" },
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ description | jsonEscape | safe }}"
        },
        {
            "@type": "{{ siteconfig.authorType }}",
            "@id": "{{ pageUrl }}#profile",
            "name": "{{ publisherName | jsonEscape | safe }}",
            "url": "{{ '/' | absoluteUrl }}",
            {% if publisherData.bio %}"description": "{{ publisherData.bio | jsonEscape | safe }}",{% endif %}
            {% if publisherData.email %}"email": "{{ publisherData.email | jsonEscape | safe }}",{% endif %}
            {% if publisherSameAs | length %}"sameAs": {{ publisherSameAs | dump | safe }},{% endif %}
            "{{ 'image' if siteconfig.authorType == 'Person' else 'logo' }}": {
                "@type": "ImageObject",
                "url": "{% imageTransformAbsoluteUrl publisherImage, 1200, 'webp' %}"
            }
        }
        {% else %}
        ,{
            "@type": "WebPage",
            "@id": "{{ pageUrl }}#webpage",
            "name": "{% if title %}{{ title | jsonEscape | safe }} - {% endif %}{{ siteconfig.title | jsonEscape | safe }}",
            "url": "{{ pageUrl }}",
            "isPartOf": { "@id": "{{ '/' | absoluteUrl }}#website" },
            "inLanguage": "{{ siteconfig.lang }}",
            "description": "{{ description | jsonEscape | safe }}"
        }
        {% endif %}
    ]
}
</script>
{% endif %}