<div class="postpagination">
	{% if pagination.hrefs.length > 1 %}
	<ul class="pagination flex">
	    {% if pagination.pageNumber > 0 %}
	    <li>
			<div class="leading-relaxed">
				<a href="{{ pagination.href.first }}" class="block">&lt;&lt;</a>
			</div>
		</li>
	    <li>
			<div class="leading-relaxed">
				<a href="{{ pagination.href.previous }}" class="block">&lt;</a>
			</div>
		</li>
		{% endif %}
		{% if pagination.pageNumber < 2 %}{% set startWith = 0 %}{% else %}{% set startWith = pagination.pageNumber - 2 %}{% endif %}
	    {% for pageHref in pagination.hrefs %}
		{% if loop.index0 < 5  and pagination.hrefs[loop.index0 + startWith] %}
	    <li>
			<div class="{% if pagination.hrefs[loop.index0 + startWith] === page.url %} active{% endif %}">
				<a href="{{ pagination.hrefs[loop.index0 + startWith] }}" class="block">{{ loop.index + startWith }}</a>
			</div>
		</li>
		{% endif %}
		{% endfor %}
		{% if (startWith + 5) < pagination.hrefs.length %}
	    <li>
			<div class="leading-relaxed">
				<a href="{{ pagination.href.next }}" class="block">&gt;</a>
			</div>
		</li>
	    <li>
			<div class="leading-relaxed">
				<a href="{{ pagination.href.last }}" class="block">&gt;&gt;</a>
			</div>
		</li>
		{% endif %}
	</ul>
	{% endif %}
</div>